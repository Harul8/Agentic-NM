# Core / runtime requirements for Nyaymalaw

# API server
fastapi==0.111.0
# Let FastAPI / CrewAI choose compatible uvicorn / pydantic versions
email-validator==2.1.1

# CrewAI agents + LLM resolution
# Install CrewAI with LiteLLM extra so provider-style models
# (e.g. ollama/...) initialize reliably.
crewai[litellm]==1.10.1
apscheduler==3.10.4

# HTTP
requests==2.32.3
tiktoken
# Let FastAPI / CrewAI pull a compatible httpx/httpcore/h11 trio

# Vector search & embeddings
faiss-cpu==1.8.0
orjson>=3.9
numpy==1.26.4
sentence-transformers==2.7.0
huggingface-hub>=0.34,<1.0

# CUDA-enabled PyTorch defaults for Linux/WSL.
# pip will use this wheel index for packages not found on PyPI.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124; platform_system == "Linux"
torchvision==0.21.0+cu124; platform_system == "Linux"
torchaudio==2.6.0+cu124; platform_system == "Linux"

# Non-Linux fallback (Windows/macOS/other).
torch>=2.6,<2.7; platform_system != "Linux"
torchvision>=0.21,<0.22; platform_system != "Linux"
torchaudio>=2.6,<2.7; platform_system != "Linux"

# PDF processing (lightweight runtime helpers)
# Let CrewAI / ingestion stack control pdfplumber version
pypdf==4.2.0
# Word document text extraction (upload feature)
python-docx>=1.1.0
# PDF page rendering for scanned document OCR (no external binary required)
pymupdf>=1.24.0

# Web scraping (Ingestion / case law)
beautifulsoup4==4.12.3
# Use a ddgs version compatible with Python >=3.10
ddgs==9.11.4

# Progress & utilities
tqdm==4.66.4
# Let CrewAI choose compatible python-dotenv version

